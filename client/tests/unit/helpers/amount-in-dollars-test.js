
import { amountInDollars } from 'client/helpers/amount-in-dollars';
import { module, test } from 'qunit';

module('Unit | Helper | amount in dollars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = amountInDollars([42]);
  assert.ok(result);
});

